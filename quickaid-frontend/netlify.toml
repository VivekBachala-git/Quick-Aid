# netlify.toml

[build]
  command = "npm run build && npx next export"
  publish = "out"

# Redirect all routes to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
